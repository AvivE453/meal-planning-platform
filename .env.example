DATABASE_URL="postgresql://meal_planning:meal_planning@localhost:5432/meal_planning?schema=public"
REDIS_URL="redis://localhost:6379"

# Free tier: developer.edamam.com -> create a "Food Database API" application.
# Nutrition search falls back gracefully with a clear error until these are set.
EDAMAM_APP_ID=""
EDAMAM_APP_KEY=""

# Dev-only placeholder — generate a real random value for anything beyond local dev,
# e.g. `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
JWT_ACCESS_SECRET="0637d4c9b56b5c1fcb9c95a40fefca38d38e29a3627a94368a9eeb9dec5e79a3"
JWT_ACCESS_TTL="15m"
REFRESH_TOKEN_TTL_DAYS="30"
